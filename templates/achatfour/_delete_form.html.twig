<form method="post" action="{{ path('achatfour_delete', {'seqresa': achatfour.seqresa}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ achatfour.seqresa) }}">
    <button class="btn">Delete</button>
</form>
