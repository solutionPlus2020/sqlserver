{% extends 'base.html.twig' %}

{% block title %}Niveau{% endblock %}

{% block body %}
    <h1>Niveau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Seqniveau</th>
                <td>{{ niveau.seqniveau }}</td>
            </tr>
            <tr>
                <th>Libniveau</th>
                <td>{{ niveau.libniveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('niveau_index') }}">back to list</a>

    <a href="{{ path('niveau_edit', {'seqniveau': niveau.seqniveau}) }}">edit</a>

    {{ include('niveau/_delete_form.html.twig') }}
{% endblock %}
