<form method="post" action="{{ path('utilisateur_delete', {'sequtil': utilisateur.sequtil}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.sequtil) }}">
    <button class="btn">Delete</button>
</form>
