{% extends 'base.html.twig' %}

{% block title %}Agents{% endblock %}

{% block body %}
    <h1>Agents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>AuId</th>
                <td>{{ agent.auId }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ agent.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ agent.prenom }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ agent.login }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ agent.mdp }}</td>
            </tr>
            <tr>
                <th>Codeagence</th>
                <td>{{ agent.codeagence }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ agent.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('agents_index') }}">back to list</a>

    <a href="{{ path('agents_edit', {'auId': agent.auId}) }}">edit</a>

    {{ include('agents/_delete_form.html.twig') }}
{% endblock %}
